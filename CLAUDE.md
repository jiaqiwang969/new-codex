# Claude Code Notes

This repository uses `AGENTS.md` as the single source of truth for agent instructions.

- Read and follow `AGENTS.md` before making changes.
- When working in a subdirectory, note that Codex may concatenate multiple `AGENTS.md`
  files from the repo root down to the working directory.

